const Input = (props) => {

    /*      CREACIÓN DE UNA ETIQUETA DIV COMO COMPONENTE*/
    return (

        <div id="result" className="row form-control"  >{props.result_}</div>
    )
}

export default Input